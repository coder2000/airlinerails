language: ruby
rvm:
  - 2.5.3
before_script:
  - bundle exec rails db:create --all
  - bundle exec rails db:migrate
script:
  - bundle exec rails ci:tests
services:
  - postgresql
notifications:
  email: false
